{
  "name": "meds-widget-firestore-rules-tests",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.0.0"
  },
  "scripts": {
    "test": "firebase emulators:exec --project demo-meds-widget-rules --only firestore \"node --test --test-concurrency=1 test/*.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.1",
    "firebase": "12.17.0"
  }
}
